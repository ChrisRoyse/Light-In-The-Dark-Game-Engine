# SITREP #2 — Light in the Dark / Ashen Reach
**Timestamp:** 2026-06-26 15:58:10 (local)
**Author:** Claude (Opus 4.8) → handoff to a fresh-context agent
**Supersedes:** SITREP_2026-06-26_113948.md (committed in `03fd7b9`). Read this one; the architecture changed *massively* since the last SITREP.

---

## 0. TL;DR (read this)
- Repo = ① an AI-native no-code game **engine** + ② an original flagship game **"Ashen Reach"** built with it. **State lives in GitHub Issues** (`Light-in-the-Dark-Analytics/light-in-the-dark-game-engine`); start at pinned **#780**. **477 open issues** (up to ~#1164).
- **HUGE PIVOT this session (the headline):** we **dropped g3n** and committed to building **our own proprietary game engine in Rust, BUILT ON TOP OF Calyx**, with Calyx as the **backend knowledge layer**. ~90 new engine issues created (#1071–#1164). **None implemented — it is a spec, not code.**
- **Two phases of REAL work exist, both unfinished:** (A) **Calyx runtime bring-up** (embedders are 100% local now; the Blackwell sm_120 ORT stack is NOT built yet). (B) **The Rust engine** (zero lines written; cargo workspace not scaffolded).
- **Git is clean and pushed to BOTH remotes** (`origin` = Analytics working repo, `public` = ChrisRoyse mirror), `main` @ `03fd7b9`, 0/0 on both. The only uncommitted thing is this SITREP.
- **Be honest with the operator:** there is a beautiful, exhaustive *vision* (78-image README, ~477 issues) and almost **zero implementation**. The gap is the whole project.

---

## 1. Git status & history
- Branch `main`. Working tree clean except `tmp/` (this SITREP, untracked). **0 behind / 0 ahead** of BOTH `origin` and `public`.
- Recent commits are ALL docs/images: `03fd7b9` (SITREP #1) ← `a1ffbf8` (Hell section + 8 imgs) ← `efc6a90` (race/class rosters) ← `c7d901b` (70-image README) ← image batches. **No engine/Calyx CODE has been committed this entire session.** The README + 78 images in `docs/readme/` are the only working-tree changes all session.
- **Two remotes (set up this session):** `origin` → Analytics (working, where the 477 issues live), `public` → `ChrisRoyse/Light-In-The-Dark-Game-Engine` (public mirror, content-only — issues do NOT mirror). Sync the public face with `git push public main`.

## 2. The architecture, AS OF NOW (binding — supersedes everything older)
Read these doctrines in order: **#1071** (custom Rust engine on Calyx), **#915** (engine IS an MCP server), **#777** (Calyx usage, now "Calyx is the backend layer"), **#884/#885** (visual storytelling #1 / UX), **#995** (multiplayer PvP full-loot RP game design), **#800** (FSV-against-reality engineering), **#928** (dual perf target), **#1055** (original world bible).
```
CALYX  — knowledge + backend layer (Rust, GPU, 50 lenses): Aster DB, search, dedup,
         provenance, Assay/Lodestar/Oracle, Ward, the AGENT RUNTIME, orchestration
   │  the ENGINE is BUILT ON Calyx (queries it for everything)
   │  ── LOWERING VALVE (one-way, frozen, fingerprinted) ──►
ENGINE (Rust, on Calyx): deterministic SIM (fixed-point, zero-alloc) + wgpu RENDERER
   │  ↑ ONLY the hot sim tick + render frame are excluded from live Calyx calls
EDITOR (egui, on engine + Calyx) = the product + an MCP server
   └─► ASHEN REACH built ENTIRELY IN the Editor (the proof it works, #959)
```
- **Dropped:** g3n (#861/#873 closed/superseded) and the Go sim (`light-in-the-dark-game-engine/` is reference only). **All-Rust now. No cgo.**
- **Engine epics:** mega #1072; subsystems → foundation #1074, core/ECS #1080, **renderer(wgpu) #1085**, assets #1098, animation #1104, physics #1109, **Calyx backend #1113**, audio #1125, vfx #1128, deterministic sim #1132, **editor(egui) #1137**, MCP #1143, quality+acceptance #1146 (fully-operational gate #1150). Full Calyx utilization #1151. Dep/license policy (permissive-only) #1073.
- **Tech stack (all MIT/Apache, our code proprietary on top):** wgpu (= Vulkan on this box) · WGSL (our shaders) · winit · glam · egui · hecs/custom-ECS · rapier · kira · gltf. **Vulkan is already in (via wgpu)**; **DLSS skipped** as a core dep (free but proprietary, NVIDIA-only) — open-source FSR/NIS/DLAA is the default upscaling path (#1152). Raw Vulkan (`ash`) is a future escape hatch only.
- **Quality contract (#1146/#1147/#1164):** every produced game must look **AT LEAST** as good as `docs/readme/*.png` (stylized real-time PBR, Warcraft-class), in 2D (#1154) or 3D, and it must be **effortless** — engine ships the look by DEFAULT (#1164).
- **Dual perf target (#928, RESOLVED a prior contradiction):** Editor maxes the workstation (9950X3D/5090/128GB); **produced GAMES must run on a $500–$1,000 player PC** (~Ryzen5/RX6600/GTX1660/16GB @ 1080p60), scaling to ultra. Min-spec scalability #1153.
- **Game design (#995):** Ashen Reach = multiplayer, PvP, full-loot, roleplay-ENFORCED, permadeath. Original world (#1055): realm of Vhael, 7 gods, 7 factions, the Infernal Deeps end-game (#1063). Mortality/permadeath/aging #1060, undeath #1061, Hall of the Remembered #1065. RP-quality drives progression (#982/#983). Self-hosted servers (#1037) + a **Battle.net-style ecosystem** (#1155: publish/share/discover/play each other's games).
- **Copyright boundary (IMPORTANT):** Ashen Reach is ALL ORIGINAL. Carrion Fields is a MECHANICS-only benchmark — never copy its lore/names/prose into issues, the game, or the README.

## 3. The Calyx runtime / embedder extraction phase — REAL state
**DONE + FSV-verified:** all 50 lenses are local at `C:\calyx` (**38.98 GB**); registry paths rewritten Linux→Windows; **50/50 manifests resolve** (incl. 8 lenses whose weights lived in aiwonder `fsv/` dirs — I caught + copied those). Documented on #773. Robust resumable transfer scripts in scratchpad (`transfer2.ps1`, `transfer-fsv.ps1`).
**NOT done — the gating blocker:** the **Blackwell sm_120 ONNX Runtime stack**. FSV just now: `calyxd.exe` is built (`C:\code\Calyx-Dev\target\release\`), but **`onnxruntime.dll` is MISSING**, **`nvcc` NOT on PATH** (no CUDA toolkit), Python present. Plan (SITREP #1 §5 still valid): install CUDA 13.x + cuDNN 9 → get/build `onnxruntime.dll` 1.26 with sm_120 (official wheels lack sm_120 → likely a from-source build) + Python `onnxruntime-gpu` sm_120 for the 13 multimodal adapters → set env (`CALYX_HOME=C:\calyx`, `ORT_DYLIB_PATH`) → `calyxd --audit-vram` → warm-load → **read a real embedding back** (the only acceptable "works"). Open decision: 2 `tei_http` lenses (HTTP-served, not local) — run TEI / substitute ONNX / drop.

## 4. Green flags 🟢
- Git clean + pushed to both repos. Roster 100% local + verified. Calyx already builds/runs on Windows (calyxd.exe). Vision is exhaustively documented; a fresh agent can onboard from #780. Operator is decisive; doctrine (#800) is excellent (FSV-against-reality). The g3n→Rust pivot is the *right* call (one language, on Calyx, full control) and is now cleanly captured with no contradictions left in the masters.

## 5. Red flags 🔴
- **~Zero implementation.** 477 issues, 78 images, 0 lines of engine/Calyx code written this session. The project is a spec + a pitch. Do not let the polish imply a working system.
- **Two big build fronts, both unstarted:** the sm_120 ORT stack (fiddly, possibly hours-long compile) AND a from-scratch Rust engine (months of work). Sequence them; don't thrash.
- **Scope is enormous and still growing** (the operator adds vision rapidly). Anchor hard on #780 + the engine mega-epic #1072 + the §6 plan. Resist generating *more* content/images — there is more than enough.
- **PowerShell gotchas that bit me:** (1) `"...#$var:..."` → a `:` right after a variable is a PARSE error (script doesn't run at all) — use `${var}`; ALWAYS FSV issue creation by reading back the highest issue number. (2) a helper named `NI` shadowed the `New-Item` alias and silently created files instead of running `gh`. (3) foreground `Start-Sleep` is blocked by the harness — use a Bash until-loop to wait on a condition.
- **Determinism tension:** "engine built on Calyx" must NOT mean Calyx in the hot sim/render path. The lowering valve is load-bearing; preserve it (#1071, #1113).

## 6. Recommendations / next steps (in order)
1. **Decide the front with the operator:** (a) finish the **Calyx runtime bring-up** (sm_120 ORT → first real embedding on the 5090) — the smaller, higher-certainty win; or (b) **scaffold the Rust engine** (#1075: cargo workspace; then a wgpu "hello triangle" on Calyx; then a PBR cube; then the reference scene matching the README). I'd do **(a) first** (it unblocks the AI tier and is bounded), then start (b).
2. For (a): install CUDA 13.x + cuDNN 9; source-build ORT 1.26 with `CMAKE_CUDA_ARCHITECTURES=120` (or adapt a Blackwell prebuilt); wire env; `calyxd --audit-vram`; warm-load; FSV an embedding. Resolve the 2 tei_http lenses.
3. For (b): `cargo new` workspace per #1075 (engine/editor/game crates, cargo-deny license gate #1073); winit window + wgpu surface; PBR forward renderer → Forward+ + CSM + post (bloom/tonemap) → the README reference-scene acceptance (#1147). Keep engine→Calyx dependency direction.
4. Keep committing + pushing to BOTH remotes; keep issues current (state lives there). FSV-in-reality before claiming anything works (#800).

## 7. Personal, transparent take
The operator is building something genuinely ambitious and is moving at the speed of imagination — this session they re-architected the whole engine (Go/g3n → Rust-on-Calyx), defined a Battle.net-style ecosystem, and nailed down the quality+perf contract, all on top of a 78-image illustrated README and an original MUD-deep RPG world. The *thinking* is now world-class and fully documented. But I have to be blunt for the next agent: **none of it is built.** The "extraction phase" (Calyx running locally) is ~80% through groundwork and faces a real CUDA/ORT compile; the engine is a greenfield Rust project that hasn't begun. The single most valuable thing anyone can do next is convert *some* of this spec into one FSV'd, real, running artifact — a warm embedding on the 5090, or a wgpu window rendering a PBR scene at README quality. Pick one, build it for real, verify it against reality, commit it. Momentum on *implementation* is what this project needs now, not more vision. The vision is done; the building has not started.

---
*End SITREP #2. Next agent: read #780 + #1071, then §3 + §6. Make one real thing run and FSV it.*
