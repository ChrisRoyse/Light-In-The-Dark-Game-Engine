# new-capabilities.txt — reverse-closure whitelist for jassgen -revclosure (#260).
#
# Reverse closure: every EXPORTED func/method in litd/api must trace to either
#   (a) a goMapping symbol in api-manifest.json (a deduplicated JASS port), or
#   (b) a rule in this file (a LitD-original capability or structural plumbing).
# Anything in neither set is unaccounted scope and FAILS the gate. Exported
# types/vars (data structures, options/event structs, enums) are out of scope —
# this gate governs the BEHAVIORAL surface (callable verbs), not data shapes.
#
# Rule forms (first match wins; '#' starts a comment; blank lines ignored):
#   suffix  <.Method>     any "<Type>.<Method>" method with this name
#   prefix  func <Pfx>    any package func whose name starts with <Pfx>
#   exact   <Symbol>      one exact "Type.Method" or bare "Func"
#
# Grandfathered surface as of #260: the entries below are the known new-API verbs
# at M2. A NEW export not covered here (or by a new manifest mapping) trips CI —
# which is the point: it forces every added verb to be either a JASS port or a
# consciously-declared new capability.

# --- mechanical handle hygiene (R-API-5/6: every noun handle carries these) ---
suffix  .Valid
suffix  .IsZero

# --- functional options (R-API-3): With* option constructors, New* builders ---
prefix  func With
prefix  func New

# new-noun: Missile (R-SIM-7 deterministic projectile; no JASS equivalent)
exact   Event.Missile
exact   Game.SpawnMissile
exact   Missile.Detonate
exact   Missile.Expire
exact   Missile.Owner
exact   Missile.Position
exact   Missile.SetTarget
exact   Missile.Source
exact   Missile.Target

# new-noun: Storage (typed deterministic save/load; supersedes JASS gamecache)
exact   Storage.GetBool
exact   Storage.GetReal
exact   Storage.GetString
exact   Storage.Load
exact   Storage.Save
exact   Storage.SetBool
exact   Storage.SetReal
exact   Storage.SetString

# new-noun: Buff (typed buff/status accessors)
exact   Buff.Present
exact   Buff.RemainingSeconds
exact   Buff.Remove
exact   Buff.Stacks
exact   Buff.Type
exact   Game.BuffType
exact   Unit.ApplyBuff

# new-noun: Attachment (per-handle attachment table)
exact   Attachment.Get
exact   Attachment.Has
exact   Attachment.Remove
exact   Attachment.Set

# event surface: payload accessors + sink installers (presentation/event pattern)
exact   DamageEvent.Amount
exact   DamageEvent.Source
exact   DamageEvent.Unit
exact   Event.Kind
exact   EventView.Damage
exact   EventView.Kind
exact   EventView.OwnerPlayer
exact   Game.OnAudio
exact   Game.OnCamera
exact   Game.OnDamage
exact   Game.OnDefeat
exact   Game.OnEvent
exact   Game.OnInvalidHandle
exact   Game.OnUI
exact   Game.OnUIScreen
exact   Game.OnVictory
exact   Subscription.Cancel
exact   UnitView.OwnerPlayer
exact   UnitView.Position

# g.UI() screen surface (#526, R-UI-1): LitD-original menu/screen builder — a
# presentation sink like OnUI, no WC3 JASS native (the JASS frame/dialog family
# is tombstoned deferred-v2).
exact   Game.UI
exact   UI.Show
exact   UI.Hide

# handle marshaling seam (#267, decision #385): the opaque handle<->HandleRef
# codec the Lua binding layer and the coroutine save system (#264) cross.
# Structural plumbing, no JASS equivalent. (HandleRef/HandleKind/Handle are
# exported types — out of this gate's verb scope.)
exact   RefOf
exact   Game.Resolve

# public setup path (#386/#387): construct + seed a game from outside the api
# package. NewGame is covered by `prefix func New`; DefineUnits seeds unit data
# (supersedes JASS slk/data-table loading) — structural, no single JASS verb.
exact   Game.DefineUnits

# non-unit data-table install seams (#394): the bulk companions to DefineUnits,
# seeding the rest of a world's data set (effect arena, abilities, items, buffs,
# tech upgrades) from outside the api package. Each wraps the sim's existing
# Bind* method; like DefineUnits these supersede JASS slk/data-table loading and
# have no single JASS verb — structural setup plumbing.
exact   Game.DefineEffects
exact   Game.DefineAbilities
exact   Game.DefineItems
exact   Game.DefineBuffTypes
exact   Game.DefineUpgrades

# combat install seam (#406): the damage-coefficient matrix (coeff[attackType]
# [armorType], thousandths) combat resolution multiplies each hit by. Wraps the
# sim's BindDamageMatrix; structural setup like DefineEconomy, supersedes JASS
# slk gameplay-constant config. Until bound, every hit is dropped — so a world
# that wants damage seeds it here.
exact   Game.DefineCombat

# economy install seam (#396): initialises the per-player resource ledger that
# the SetGold/SetLumber/SetResource verbs write into. Wraps the sim's
# BindEconomy; structural setup, supersedes JASS slk economy config.
exact   Game.DefineEconomy

# hero rule-set install seam (#396): installs the XP curve, bounty table, hero
# defs + skill trees, attribute coefficients, and revive costs. Wraps the sim's
# BindHeroes; structural setup, supersedes JASS slk hero config.
exact   Game.DefineHeroes

# resource-node install + spawn seam (#401): the node-type registry companion to
# DefineUnits (so a world can ship gold mines / harvestable nodes), the code
# resolver (mirrors Game.UnitType), and the spawn verb. Wraps the sim's
# BindResourceNodeDefs/CreateResourceNodeByID. Spawning resource nodes is a LitD
# capability — WC3 places mines via map data, not a script native.
exact   Game.DefineResourceNodes
exact   Game.ResourceNodeType
exact   Game.CreateResourceNode

# order-verb resolver (#267): the Game-bound way a script obtains an order verb
# from the catalog (OrderMove/Attack/…) without ambient globals — analogue of
# Game.UnitType. Pairs with the generated Unit.Order (D3 collapse of the JASS
# IssueXxxOrder families). No single JASS native — the order catalog is a LitD
# dedup landing point.
exact   Game.Order

# runtime world-loader seam (#268): the api stays Lua-agnostic — a backend
# (litd/luabind) registers via SetWorldLoader, and LoadWorld delegates to it to
# load a world directory at runtime. HasWorldLoader reports whether a backend is
# installed. Structural plumbing (a LitD original), no JASS equivalent.
exact   Game.LoadWorld
exact   Game.SetWorldLoader
exact   Game.HasWorldLoader

# verification seam (#267/#271): a deterministic uint64 digest of authoritative
# sim state, for Go-vs-Lua binding conformance and run-to-run determinism.
# Structural, no JASS equivalent (a leak-free uint64, no sim types exposed).
exact   Game.StateHash

# host-loop primitive (#268/#269): advance the deterministic sim by N ticks,
# resuming scheduled threads/timers at their wake tick. The world loader and Lua
# execution track drive game time through it. Structural, no single JASS verb.
exact   Game.Advance

# value-math (R-API-2 value types; no JASS native)
exact   Angle.Normalized
exact   Angle.Radians
exact   Rad
exact   Vec2.Sub

# functional options / helper verbs (R-API-3 option constructors)
exact   AfterUnits
exact   At
exact   For
exact   ForPlayer
exact   FromSource
exact   PanHeight
exact   PanOver
exact   PolledWait
exact   SharedVision
exact   Started
exact   TargetNone
exact   TargetPoint
exact   TargetUnit
exact   Teleport
exact   UseAt
exact   UseOn
exact   Where

# camera cinematic control (new-capability beyond JASS camera natives)
exact   Camera.BeginCinematic
exact   Camera.EndCinematic
exact   Camera.Following
exact   Camera.InCinematic
exact   Camera.SetZoom

# D4 helpers library (#258): the logic-bearing blizzard.j BJ survivors, kept
# once in litd/api/helpers (+ helpers/melee), reimplemented on the public api.
# Their forward-closure JASS disposition is recorded in overrides.toml; they
# are accounted here for reverse closure because the gate's manifest match is
# package=litd/api + bare-symbol, while helper verbs live in sub-packages
# (same treatment as PolledWait above). helpers verbs:
exact   CreateUnits
exact   WeightedChoice
exact   RandomItemType
exact   Gate
exact   Elevator
exact   Transmit
# helpers/melee data-driven setup library:
exact   LoadFaction
exact   LoadFactionBytes
exact   StartingResources
exact   StartingUnits
exact   VictoryDefeatConditions
exact   Standard
exact   PlayerUnitCount

# cooperative script threads (#377): the green-thread execution model over
# the stackless scheduler. The canonical JASS PolledWait/TriggerSleepAction
# port is helpers.PolledWait (D4); these are the LitD primitives it rides —
# the thread constructor, the in-thread suspend verb, and the ambient/save
# introspection (execution-model.md §2, R-EXEC-1).
exact   Game.Run
exact   Thread.PolledWait
exact   CurrentThread
exact   Game.SuspendedThreadCount

# engine control (new-capability: registry/scheduler/debug surface)
# Game.AttachMeleeAI installs the litd/ai/melee RTS controller as a live computer
# player (M5.5). LitD-original, no JASS native: WC3's computer players are the
# external .ai script engine (StartMeleeAI/CommandAI operate that VM), not an
# in-language attach of a strategy object — this is the engine's own AI-domain
# wiring (R-EXEC-3), sibling to the manifest-mapped Game.AttachAI.
exact   Game.AttachMeleeAI
exact   Game.DetachAI
exact   Game.ElapsedTime
exact   Game.Every
exact   Game.IsAIPlayer
exact   Game.IsReplay
exact   Game.Localize
exact   Game.RegisterAbility
exact   Game.RegisterEffectModel
exact   Game.Resume
exact   Game.SetDebug
exact   Game.SetLocalPlayer

# typed state accessors (fine-grained split of mapped Get/SetXxxState families)
exact   Ability.Cooldown
exact   Ability.ManaCost
exact   Doodad.Hide
exact   Doodad.Promoted
exact   Doodad.Show
exact   Force.Count
exact   Game.AllPlayers
exact   Game.AppendUnitsIn
exact   Game.AppendUnitsInRange
exact   Game.Camera
exact   Game.Doodad
exact   Game.ItemType
exact   Game.NeutralExtra
exact   Game.NeutralVictim
exact   Game.SetUpkeep
exact   Game.TimeOfDaySuspended
exact   Game.TransferResource
exact   Game.UnitType
exact   Item.Carried
exact   Item.Carrier
exact   Player.AlliedVictory
exact   Player.FoodCap
exact   Player.FoodUsed
exact   Player.GoldLostToUpkeep
exact   Player.GoldUpkeepRate
exact   Player.LostToUpkeep
exact   Player.Lumber
exact   Player.LumberLostToUpkeep
exact   Player.LumberUpkeepRate
exact   Player.Result
exact   Player.SetAlliance
exact   Player.SetAlliedVictory
exact   Player.SetFoodCap
exact   Player.SetLumber
exact   Player.UpkeepRate
exact   Table.Len
exact   Unit.EnableInventory
exact   Unit.IsHero
exact   Unit.Mana
exact   Unit.SetMana
exact   Unit.SwapItems
exact   UnitSet.AppendUnits
exact   UnitSet.Compact

# lockstep net seam (#68): the thin api the deterministic-network layer (litd/net)
# drives — current tick, stage one encoded command record, ingest the staged
# batch between ticks, and a per-record consumption hook for desync verification.
# Records cross as encoded bytes (no sim types leak). LitD-original lockstep
# plumbing; WC3 had no script-level command-staging surface.
exact   Game.Tick
exact   Game.StageCommand
exact   Game.IngestCommands
exact   Game.OnCommandRecord

# mid-game save/load seam (#204/#270): write/read a deterministic match snapshot
# (entity state + the stackless scheduler's suspension records) across an io byte
# stream, fingerprint-tagged. Called between ticks. Supersedes JASS gamecache
# (the typed Storage noun) — these persist the whole match, no JASS equivalent.
exact   Game.SaveState
exact   Game.LoadState

# map-data exposure (#410): read the loaded map's authored placements — the map
# handle, start locations, and beacon control-points — so worlds read the map
# instead of hardcoding. WC3 exposes map data through the editor / map file, not
# a script native; this is the LitD read seam.
exact   Game.MapData
exact   Game.MapStarts
exact   Game.MapBeacons

# Lua coroutine scheduler seam (#269/#270): the api-side hooks the luabind layer
# binds — register a resume continuation id and post a descriptive (serializable)
# script wake record on the stackless scheduler. Structural plumbing for the
# deterministic green-thread model (execution-model.md §7), no JASS equivalent.
exact   Game.RegisterScriptCont
exact   Game.AfterScript

# Lua coroutine event-wait seam (#413): the api-side hooks the luabind WaitForEvent
# binds — register the single reserved-HandlerID script event dispatcher (at VM
# setup, before any load) and subscribe it to a kind so a coroutine can park on an
# EVENT (unit death, order, region enter), not only a timer. The kind→handler
# subscription is serialized sim state, so an event wait round-trips a save/load.
# Structural plumbing for the green-thread model (execution-model.md §7), no JASS
# equivalent (JASS waits are timer-only — TriggerSleepAction).
exact   Game.RegisterScriptEventDispatcher
exact   Game.SubscribeScriptEvent

# determinism/verification seam (#271): the per-system hash breakdown behind
# Game.StateHash — a snapshot of the top digest plus each subsystem's sub-digest,
# and the matching ordered system names — for localizing a desync. Leak-free
# (uint64s + names, no sim types). LitD-original, no JASS equivalent.
exact   Game.HashSnapshot
exact   HashSystemNames

# audio cue identity seam (#428): the canonical cue-string → handle hash (FNV-32a)
# shared by Game.CreateSound and the audio sound-classification table, so the
# Manager can recover a playing cue's domain/priority from an AudioEvent.Cue. A
# pure deterministic hash, no sim types; LitD-original, no JASS equivalent (JASS
# sound handles are opaque engine pointers).
exact   CueID

# render presentation-cue stream (#313, #449): the per-tick render-event accessor
# behind the audio sound trigger / death anim — drains one-shot cues (unit death,
# …) from the published snapshot as value types (no sim/G3N leak). Read-only
# against the NON-HASHING render channel, so a consumer cannot perturb determinism.
# LitD-original, no JASS equivalent (JASS has no render-event surface — sounds are
# fired imperatively from triggers).
exact   Game.RenderEvents

# --- ECA Trigger substrate (ADR #451, #461): the first-class Trigger noun.
# These reintroduce the WC3 trigger verbs (CreateTrigger / TriggerRegisterX /
# TriggerAddCondition / TriggerAddAction / Enable/Disable / TriggerExecute /
# TriggerEvaluate / DestroyTrigger) as the engine event substrate; OnEvent
# becomes sugar over them (#462). Conditions/actions live in the sim core as a
# boolexpr arena + handler refs, not as public types.
exact   Game.NewTrigger
exact   Trigger.On
exact   Trigger.When
# WhenEvent is the script compile target for TriggerAddCondition (#463): a
# condition that receives the full Event (the GetTriggerUnit idiom) rather
# than the pure EventView that Trigger.When exposes.
exact   Trigger.WhenEvent
exact   Trigger.Do
exact   Trigger.Enable
exact   Trigger.Disable
exact   Trigger.IsEnabled
exact   Trigger.Execute
exact   Trigger.Evaluate
# Every arms a periodic-timer event on the trigger (#464): the serializable
# substrate under the script Game_Every, replacing the Go-closure timer.
exact   Trigger.Every
exact   Trigger.Destroy
exact   Trigger.SetInitiallyOn
# trigger-scope option constructors + the ready-made owner condition
exact   ForUnit
exact   OwnedBy
exact   InRegion
exact   OwnerIs
# ability/attack/buff lifecycle event surface (#470 — exposes #467/#468/#469)
exact   Event.Ability
exact   Game.OnAbilityCast
exact   Game.OnAttack
exact   Game.OnBuffApplied
# data-driven extensible attack/armor type tables (#472)
exact   Game.DefineDamageTypes
exact   Game.AttackTypeID
exact   Game.ArmorTypeID
# programmable combat: damage-formula setters + full read/write DamageEvent (#475)
exact   Game.ReplaceDamageStage
exact   Game.SetDamageFormula
exact   Game.SetArmorReduction
exact   DamageEvent.RawAmount
exact   DamageEvent.AttackType
exact   DamageEvent.ArmorType
exact   DamageEvent.SetAttackType
exact   DamageEvent.SetArmorType
exact   DamageEvent.Flags
exact   DamageEvent.ApplyCoefficient
# live per-instance weapon overrides (#476): re-arm a unit's weapon at runtime.
# JASS analog is the BlzSetUnitWeapon* setter family (collapsed to one field
# enum). Hashes + serializes like the #353 ability-field overrides.
exact   Unit.SetWeaponField
exact   Unit.ClearWeaponField
exact   Unit.ClearWeapon
exact   Unit.WeaponField
# runtime effect-primitive registration (#477): a modded map registers a named
# effect/Action in setup and invokes it from a trigger Action. No JASS native —
# the registry is a LitD extensibility seam (relates to gamecache-free data).
exact   Game.RegisterEffect
exact   Game.RunEffect
exact   Game.EffectRegistered
exact   EffectInvocation.Source
exact   EffectInvocation.Target
exact   EffectInvocation.Heal
exact   EffectInvocation.Valid
# ability binds a trigger as its EFFECT-edge behavior (#478): bind a trigger to
# a name a data ability references. LitD authoring seam, no JASS native.
exact   Trigger.BindName
# public cast-ability order + script render cue (#479): the dogfood of a full
# trigger-authored spell surfaced these gaps. Unit.Cast issues the cast order
# (IssueTargetOrder spell family); Game.EmitSpellCue stages a non-hashing render
# VFX cue (#449 presentation channel, no JASS native).
exact   Unit.Cast
exact   Game.EmitSpellCue
# resolve a data-loaded ability's string id to its AbilityRef (#487): mirrors the
# Game.UnitType/BuffType type-catalog resolvers; LitD authoring seam, no JASS
# native (JASS abilities are 'xxxx' rawcodes passed directly).
exact   Game.AbilityRef
# buff-lifecycle trigger hooks (#480): OnBuffExpired/OnBuffRefreshed are sugar
# over OnEvent for the buff EFFECT kinds; Event.Buff/BuffStacks/FromAura decode a
# buff event's payload (type, stack count, aura-child flag). WC3 hides buffs
# behind dummy-caster abilities — no JASS native maps here.
exact   Game.OnBuffExpired
exact   Game.OnBuffRefreshed
exact   Event.Buff
exact   Event.BuffStacks
exact   Event.FromAura
# headless state-dump for full-state verification (#516): Game.DumpState writes
# the deterministic world state (entities/resources/clock) to an io.Writer so an
# agent can diff sim state directly. Debug/FSV authoring seam — JASS has no
# whole-state serializer native (SyncStored* are piecemeal gamecache I/O).
exact   Game.DumpState
# live effect enumeration + read-back for render harnesses and tools (#529):
# Game.Effects/AppendEffects list every live special effect, and Effect.Position
# reads a placed effect's location. WC3 special effects are fire-and-forget — JASS
# has no native that enumerates all effects, and BlzGetLocalSpecialEffect* are
# local-only niche reads; this is the LitD render/tooling seam (mirrors AllUnits).
exact   Game.Effects
exact   Game.AppendEffects
exact   Effect.Position
exact   Effect.ID
# production replay recording of a real melee-AI match (#404): RecordReplay turns
# on a tap that records the attached melee AI's economy/production/wave decisions
# into the .litdreplay format (sim.ReplayCommand v3); ReplayCommands/BuildReplay
# read the accumulated stream. A LitD-original determinism/observability seam —
# WC3/JASS has no replay-recording native (its replays are an engine-level binary
# format, not script-accessible).
exact   Game.RecordReplay
exact   Game.ReplayCommands
exact   Game.BuildReplay
